import ReactDOM from 'react-dom';
import ApolloApp from './ApolloApp.js'
// import { createStore } from 'redux'
// const reducer = (state, action) => {
//   switch (action.type) {
//     case 'newUser':
//       break
//   }
//   return state
// }
// const store = createStore(, 1)
// store.dispatch({
  
// })

ReactDOM.render(
  ApolloApp,
  document.getElementById('root')
);

// npm install 
// @apollo/react-hooks apollo-cache-inmemory apollo-link-http 
// apollo-client apollo-link-context

// react-router-dom semantic-ui-css semantic-ui-react